---
const selectedProgramSlug = Astro.url.searchParams.get('program') || '';

// Legacy route: keep old links working, but funnel everything through Rate My Course.
return Astro.redirect(
	selectedProgramSlug
		? `/rate-my-course?program=${encodeURIComponent(selectedProgramSlug)}`
		: '/rate-my-course'
);
---

